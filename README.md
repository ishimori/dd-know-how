# DD-Know-How

**LLMの楽観バイアスを制御し、設計判断を記録し続ける開発フレームワーク**

Claude Code のスキルとして動作する、DD（Design Document）ドリブンの開発管理システムです。

---

## 解決する問題

AIコーディングアシスタントには構造的な弱点があります。

| 問題 | 結果 |
|------|------|
| **楽観バイアス** | 「問題ありません」で見落としを素通りする |
| **コンテキスト忘却** | セッションをまたぐと設計判断の経緯が消える |
| **場当たり実装** | 全体像なしに目の前のコードだけ書く |
| **自己レビューの甘さ** | 自分が書いたコードを「正しい」前提で確認する |
| **ゴミテスト量産** | 意味のないテストコードを大量に書く |

DD-Know-How はこれらに対して、仕組みで対抗します。

## DDとは何か

DDは「意思決定の記録」「タスク管理」「作業ログ」を1つのMarkdownファイルにまとめたものです。

```markdown
# DD-042: 認証方式の変更

## 目的        ← 何を、なぜやるのか
## 検討内容    ← どんな選択肢があったか
## 決定事項    ← 最終的に何を選んだか
## タスク一覧  ← Phase分割された作業リスト（各PhaseにDA批判レビュー付き）
## ログ        ← 時系列の作業記録
## DA批判レビュー記録  ← 各Phaseで「何が壊れるか」を探した記録
```

DDはセッションをまたいでも残り続けます。3ヶ月後に「なぜこの設計にしたのか」を振り返れる。これが、コンテキストが揮発するLLMとの開発で最も価値を持つ部分です。

## 4つのアプローチ

DD作成時、タスクの性質に応じてアプローチを選択します。

| 優先順 | アプローチ | 用途 | テンプレート |
|:------:|-----------|------|-------------|
| 1 | **モック先行** | 画面の新規作成・大幅変更（見た目の合意が必要） | `dd_template.md` + `dd_template_mock.md`（差分） |
| 2 | **E2E駆動** | 画面の振る舞い中心（操作→結果の検証が必要） | `dd_template.md` + `dd_template_e2e.md`（差分） |
| 3 | **TDD** | 計算・遷移・ビジネスロジックが中心 | `dd_template.md` + `dd_template_tdd.md`（差分） |
| 4 | **標準（探索的実装）** | 上記以外 | `dd_template.md` のみ |

ユーザーが指定しなければ、CLAUDEが自動判断してDD冒頭に選択理由を明示します。Phaseごとにアプローチを混在させることも可能です（例: BE=TDD、FE=モック先行）。詳細は `templates/guides.md` を参照。

### DDの粒度＝レビューゲートの単位

**1つのDDは、1回のレビューサイクルで完結する単位にする。**

- レビューゲートが1回 → DD単体で完結
- レビューゲートが2回以上 → 親DD + 子DDに分割

```
DD-050: XX機能開発（親。管理用）
DD-050-1: DB設計（ER図レビュー→合意）
DD-050-2: 画面モック（HTMLレビュー→合意）
DD-050-3: FE/BE実装（動作確認→合意）
```

DD本体は **200行以内**。超えそうなら添付ファイル分離か子DD分割で対応。

## 核心: DA批判レビュー（Devil's Advocate）

最大の特徴は **DA批判レビュー** です。各実装フェーズの完了前に、LLMに対して批判的視点を強制します。

```
❌「確認したか？」 → 「はい、問題ありません」
⭕「どこが壊れるか？」 → 壊れる前提で探すしかない
```

### 4段階の批判手順

1. **壊れる前提で探す** — 「完了した」ではなく「何が壊れるか」と問う
2. **暗黙の前提を疑う** — 「今動いている」ではなく「前提が崩れるケースは？」
3. **将来の破壊を予測** — 「今は問題ない」ではなく「半年後の変更で壊れないか？」
4. **発見を記録** — 何も見つからないのは疑わしい。変更ファイルを読み直して再チェック

### DA品質フィルター（ノイズ削減）

| # | ルール | 内容 |
|---|--------|------|
| 1 | **検証義務** | 指摘前にコード実行 or 仕様確認で事実確認。推測だけの指摘は記録しない |
| 2 | **既知事項の除外** | `TODO`/`FIXME`、「Phase X で対応」、別DDスコープアウト済みは指摘対象外 |
| 3 | **深刻度の判定基準** | 高=今のPhaseで壊れる、中=特定条件下で壊れる、低=改善提案 |
| 4 | **再現手順の必須化** | 高/中には「何をすると→何が起きるか」を必ず記載 |

詳細は [doc/da-method.md](doc/da-method.md) を参照。

## テスト戦略: シナリオレビューゲート

LLMに任せると意味のないテストコードを量産する問題への対策。

**テストコードを書く前に、必ず自然言語でシナリオを作成しユーザーの合意を得る。** コードを先に書かない。E2Eは「人間がブラウザから手入力するのと同じこと」を再現する。

## エビデンス収集: スクリーンショットによる視覚的記録

画面を伴う実装Phaseでは、Phase完了時にスクリーンショットエビデンスを取得します。Playwright MCP の `browser_evaluate` でCSSを注入し、注目箇所を赤枠ハイライトしてキャプチャすることで、レビュー時に「どこを見ればいいか」が一目で分かります。

- **バグ修正**: Before（修正前）/ After（修正後）のペアで記録
- **新規開発**: 追加箇所をハイライトしてキャプチャ
- **配置先**: `DD-{番号}/` フォルダにDD本体と紐づけて管理

詳細は `templates/guides.md` のセクション8を参照。

## コーディング基準書

`templates/coding-standards.md` に、P01〜P22の原則を定めたコーディング基準書を同梱しています。YES/NO の二値で判定できる形式で記述されており、コードレビュー時の評価基準として機能します。

- **カテゴリ A〜G**: 型安全性、コンポーネント設計、非同期処理、API通信、レイヤー分離、バリデーション、DB操作
- **カテゴリ H**: LLM固有のアンチパターン（型システム迂回、スタブ残置、デバッグコード、SQLインジェクション）
- **採点基準**: S/A/B/C の4段階グレードで、Critical違反が1件でもあればC評価

`/setup` で導入するとプロジェクトにコピーされ、プロジェクトの技術スタックに合わせてカスタマイズできます。

## 機械検証: LLMの「やったふり」防止

LLMは「確認しました」と報告しながら実際にはコマンドを実行していないことがある。各Phaseに機械検証タスク（コマンド実行 + 期待結果の照合）を入れることで、エビデンスを強制する。

```markdown
- [ ] `shared/types.ts` の SiteRole 型を 'admin' → 'site_admin' に変更
- [ ] 🔬 `npx tsc --noEmit` で型エラーなしを確認
```

## 導入方法

### 自分のプロジェクトに導入する

```bash
cd dd-know-how
claude
> /setup /path/to/your-project
```

2段階の導入レベルから選択:

| レベル | 内容 |
|--------|------|
| Level 1 | テンプレート + ガイド。まず試したい場合 |
| Level 2 | + `/dd` スキル + DA メソッド文書。通常はこれで十分 |

手動セットアップの詳細は [IMPORT.md](IMPORT.md) を参照。

### DD-Know-How 自体を使う

```bash
git clone https://github.com/ishimori/dd-know-how.git
cd dd-know-how
claude
> /dd new ログイン機能の実装
```

## フォルダ構成

```
dd-know-how/
├── .claude/skills/
│   ├── dd/SKILL.md                # DD操作スキル（作成・参照・一覧・検索・アーカイブ）
│   └── setup/SKILL.md             # 外部プロジェクトへの導入（/setup）
├── templates/                     # ── /setup でコピーされるファイル群 ──
│   ├── guides.md                  # DD作成ガイド（アプローチ選択・命名・サイズ管理・テスト戦略・エビデンス収集）
│   ├── dd_template.md             # 標準テンプレート（共通骨格）
│   ├── dd_template_tdd.md        # TDD差分テンプレート
│   ├── dd_template_mock.md       # モック先行差分テンプレート
│   ├── dd_template_e2e.md        # E2E駆動差分テンプレート
│   ├── coding-standards.md       # コーディング基準書（P01〜P22）
│   ├── DD-INDEX.md               # DD検索インデックステンプレート
│   ├── hooks/                    # Claude Code フックテンプレート
│   │   ├── pre-edit-guard.sh     #   重要ファイル編集ガード（settings.json/.env/DD-INDEX.md）
│   │   └── post-bash-dd-archive-reminder.sh  #   アーカイブ時INDEX更新リマインダー
│   └── scripts/
│       └── dd-index-gen.sh       # DD-INDEX.md 全量再生成スクリプト
├── doc/
│   ├── da-method.md               # DA方法論ガイド（品質フィルター・再チェック条件）
│   ├── development-flow.md        # 5ステップフロー（Standard）
│   ├── development-flow-full.md   # 9ステップフロー（Full）
│   ├── spec-sync-check.md         # 仕様書同期チェック
│   ├── examples/                  # 実例集（機能実装・設計判断・バグ修正）
│   ├── customization/             # カスタマイズガイド
│   └── UPGRADE-NOTICE.md         # 既存導入プロジェクト向けアップグレード手順
├── CLAUDE.md                      # プロジェクト設定テンプレート
├── IMPORT.md                      # 手動導入ガイド
├── CHANGELOG.md                   # 変更履歴
└── LICENSE                        # MIT License
```

## カスタマイズ

- [テンプレートのカスタマイズ](doc/customization/template-sections.md)
- [開発フローのバリエーション](doc/customization/development-flow-variants.md)
- [他のAI環境での使用](doc/customization/other-environments.md)

## 設計思想

### なぜ「見落としチェック」ではなくDevil's Advocateか

初期バージョンでは「何を見落としているか？」と問う形式でした。しかし実運用で判明したのは、LLMは「見落とし」を問われると「特にありません」と答えがちだということです。

「どこが壊れるか？」と問い方を変えるだけで、LLMは壊れるポイントを探すモードに切り替わります。

### なぜ差分テンプレートか

4つのアプローチ（標準/TDD/モック先行/E2E駆動）は、標準テンプレート（`dd_template.md`）を骨格とし、アプローチ固有のPhaseだけを差分テンプレートで差し替える方式です。

```
dd_template.md（共通骨格: 目的・検討・決定・タスク・ログ）
  + dd_template_tdd.md   → TDDのPhase構成に差し替え
  + dd_template_mock.md  → モック先行のPhase構成に差し替え
  + dd_template_e2e.md   → E2E駆動のPhase構成に差し替え
  （差分なし）            → 標準（探索的実装）
```

完全独立の4テンプレートにすると共通部分の同期コストが発生するため、差分方式を採用しています。差分方式でLLMの合成精度に問題が出た場合は、独立テンプレートに切り替える判断も可能です。

## 複数人での運用

識別子プレフィックスで番号衝突を防ぎます。

```
DDI-001, DDI-002   # 石森の番号系列
DDSA-001, DDSA-002 # 斉藤の番号系列
```

## ライセンス

MIT License
